<div class="container mt-4" style="max-width: 760px;">
  <div class="mb-4">
    <h4 class="fw-bold mb-0">
      <i class="bi bi-shield-plus text-primary me-2"></i><%= t("parties.new") %>
    </h4>
    <p class="text-muted small mb-0"><%= t("parties.subtitle") %></p>
  </div>

  <%= render "form", party: @party, character_classes: @character_classes %>
</div>
